#include <stdio.h>

int main(void){

    int count = 0;

    while (count <= 1000){
        printf("\n %i - Programacao de computadores", count);
        count++;
    }
   
    /* for (count = 1; count <= 10000; count++){
        printf("\n %i - Programacao de computadores", count);
    } */
    printf("\n ");
    return 0;

}